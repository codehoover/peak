import React, { useState } from "react";
import ReactModal from "react-modal";

export default function WeightPopup(){
    const [isOpen,setIsOpen] = useState(false);
    
    return(
        <div>
            <h4> Log Weight </h4>
        </div>
    )

}